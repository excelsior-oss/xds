(* Copyright (c) xTech 1992,95.  All Rights Reserved *)
(*
 * XDS runtime library.
 * CHAR and string operations for code generated by C and X86 back-ends.
 *)
<*+ M2EXTENSIONS *>
DEFINITION MODULE ["C"] xrStrings;

IMPORT  xmRTS;
FROM SYSTEM    IMPORT  size_t, int;

PROCEDURE X2C_CAP(x: CHAR): CHAR;
PROCEDURE X2C_COPY(s: xmRTS.X2C_pVOID; s_len: size_t;
		   d: xmRTS.X2C_pVOID; d_len: size_t): xmRTS.X2C_pVOID;
PROCEDURE
<* IF (env_target="x86nt") OR (env_target="x86os2") OR (NATIVE_LIBRARY) THEN *>
         /
<* END *>
          X2C_LENGTH(s: xmRTS.X2C_pVOID; s_len: size_t): size_t;

END xrStrings.
