(* Copyright (c) 1999-2000 Excelsior, Russia. All Rights Reserved. *)
<* +M2EXTENSIONS *>
DEFINITION MODULE ["C"] xrnProf;

IMPORT rts := xProfRTS, SYSTEM;

PROCEDURE X2C_INIT_PROFILER (prof_mode: SYSTEM.INT32);
(*
   ----------------------------------------------------------------
   NOTE: to enable profiler activity BE generates the call to
   X2C_INIT_PROFILER in the BEGIN-part of the main module,
   if +GENPROF it should be called before other profiler services!!
   ----------------------------------------------------------------
*)

PROCEDURE X2C_PROFILE_BEGIN_MODULE (m: rts.X2C_Profile_Module);
(* inits profiler structures for each module
   (generated by BE in the BEGIN-part of a module, if +GENPROF *)

PROCEDURE X2C_EXIT_PROFILER (isExe: BOOLEAN);
(* flush counted profiler data (only if profiler was enabled)
   called from the finalization routine of each component (EXE & DLLs)
*)
 
END xrnProf.
