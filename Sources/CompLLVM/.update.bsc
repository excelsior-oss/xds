# Builded files to be copied into an external workspace 
# Target must be relative to the root of an external workplace
# Note: target subfolder must end with symbol "\" 
# <mode> ::= "" | "work" | "release"

# <source>               <target>        <mode>
workplace\m2llvm.exe      bin\            work